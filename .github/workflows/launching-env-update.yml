name: Update Devportal Documentation - Launching Enveloping

on:
  push:
    paths:
      - 'docs/launching_enveloping.md'

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check out the code from the triggering repository
      - name: Checkout Code
        uses: actions/checkout@v2

      # Step 2: Copy or transform README to add frontmatter for Docusaurus
 
      - name: Transform launching_enveloping.md for Docusaurus
        run: |
          mkdir transformed
          # Add header text and prepend it to launching_enveloping.md
          echo "---" > transformed/launching_enveloping.md
          echo "sidebar_position: 2" >> transformed/launching_enveloping.md
          echo "title: Launching Enveloping" >> transformed/launching_enveloping.md
          echo "---" >> transformed/launching_enveloping.md
          echo "" >> transformed/launching_enveloping.md
          echo ":::info[Note]" >> transformed/launching_enveloping.md
          echo "This content is automatically updated from the repository, in this case from the launching_enveloping.md file." >> transformed/launching_enveloping.md
          echo ":::" >> transformed/launching_enveloping.md
          echo "" >> transformed/launching_enveloping.md
          # Append the original launching_enveloping.md content after the header text
          cat launching_enveloping.md >> transformed/launching_enveloping.md

      # Step 3: Clone the Docusaurus repository
      - name: Clone Docusaurus Repository
        env:
          GITHUB_TOKEN: ${{ secrets.DOCS_TOKEN }}
        run: |
          git clone https://ivegabr:${GITHUB_TOKEN}@github.com/ivegabr/docs-test.git
          cd docs-test
          git checkout -b update-from-repo1 || git checkout update-from-repo1
          cd ..

      # Step 4: Copy the transformed README to the Docusaurus `docs` folder
      - name: Copy launching_enveloping.md to Docusaurus Repo
        run: |
          cp transformed/launching_enveloping.md docs-test/docs/rif-relay/docs/launching_enveloping.md

      # Step 5: Commit changes to the new branch in Docusaurus repository
      - name: Commit Changes
        run: |
          cd docs-test
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/rif-relay/docs/launching_enveloping.md
          git commit -m "Automated update of launching_enveloping.md from repo1"
          git push -f origin update-from-repo1

      # Step 6: Create a pull request in the Docusaurus repository

      - name: Create Pull Request
        env:
          GITHUB_TOKEN: ${{ secrets.DOCS_TOKEN }}
        run: |
          cd docs-test
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${GITHUB_TOKEN}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/ivegabr/docs-test/pulls \
          -d '{"title":"Automated update of launching_enveloping.md from repo1","body":"This PR updates the Docusaurus documentation with the latest launching_enveloping.md from repo1.","head":"update-from-repo1","base":"main"}'
